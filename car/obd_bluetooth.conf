# Bluetooth classic OBD-II adapter (ELM327 / similar SPP).
# Pair once with: sudo bash pair_obd_bluetooth.sh
# Then set the MAC below (bluetoothctl devices).

OBD_BT_MAC=00:00:00:00:00:00
OBD_BT_NAME=OBDII
OBD_RFCOMM_DEV=/dev/rfcomm0
OBD_RFCOMM_CHANNEL=1
