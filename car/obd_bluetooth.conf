# Bluetooth classic OBD-II adapter (ELM327 / similar SPP).
# This BD_ADDR is fixed for the adapter. Pair once with:
#   sudo bash pair_obd_bluetooth.sh

OBD_BT_MAC=00:1D:A5:00:5E:27
OBD_RFCOMM_DEV=/dev/rfcomm0
OBD_RFCOMM_CHANNEL=1
